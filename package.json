{
  "name": "healthy-social-solo-game",
  "private": true,
  "scripts": {
    "check": "node --check app.js && node --check chaos.js && node --check conversation.js && node --check data.js && node --check stories.js && node --check extra-stories.js",
    "test": "node --test tests/game-regression.test.js",
    "validate": "npm run check && npm run test"
  }
}
